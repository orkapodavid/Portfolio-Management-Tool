[project]
name = "starter-core"
version = "0.1.0"
description = "Core business logic package for the starter app"
requires-python = ">=3.11,<3.14"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["core"]
